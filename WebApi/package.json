{
  "name": "novelreader-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript sources for the NovelReader reading page. Compiled into wwwroot by tsc.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@microsoft/signalr": "10.0.11",
    "typescript": "7.0.2"
  }
}
